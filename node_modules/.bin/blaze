#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../blaze_compiler/bin/blaze.js" "$@"
  ret=$?
else 
  node  "$basedir/../blaze_compiler/bin/blaze.js" "$@"
  ret=$?
fi
exit $ret
