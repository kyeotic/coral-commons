var blaze = require('../src/blaze');
var Json = require('source-processor');

function testEntryParseRoot1(test) {
    var entry = blaze.AccessEntry.parse(Json.parse('{"location": "/"}'));
    test.deepEqual(entry.location, []);
    var entry = blaze.AccessEntry.parse(Json.parse('{"location": ""}'));
    test.deepEqual(entry.location, []);
    test.done();
}
exports.testEntryParseRoot1 = testEntryParseRoot1;
//# sourceMappingURL=parse_test.js.map
