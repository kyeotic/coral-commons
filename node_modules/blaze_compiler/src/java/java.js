require('source-map-support').install();

var fs = require('fs');
var TARGET = "model.java";
var DEBUG;

function generate(model, debug) {
    console.log("generating", TARGET);
    DEBUG = debug;

    var lines = [];

    generate_class(model.schema.root, lines);

    fs.writeFile(TARGET, lines.join("\n"));
}
exports.generate = generate;

function generate_class(schema, output) {
    if (DEBUG)
        console.log("generate_class", schema);

    var lines = [];
}
//# sourceMappingURL=java.js.map
